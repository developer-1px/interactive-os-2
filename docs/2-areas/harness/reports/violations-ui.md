# Violations — ui
Generated: 2026-04-12T03:24:27.995Z
Total: 6 violations in 5 files (of 357 scanned)

## By rule
| Rule | Count |
|---|---|
| ui/ 컴포넌트에서 onKeyDown/onKeyUp 바닐라 핸들링 금지 — pattern keyMap 또는 plugin.keyMap으로 선언적  | 4 |
| CSS에서 ax() 축 소유 속성 사용 금지 — ax() 또는 해당 축을 사용하세요: | 1 |
| 상태 선택자 1건 감지: | 1 |

## By file

### src/interactive-os/ui/DatePicker.tsx [M]
- ui/ 컴포넌트에서 onKeyDown/onKeyUp 바닐라 핸들링 금지 — pattern keyMap 또는 plugin.keyMap으로 선언적 키 매핑을 사용하세요. CLAUDE.md "키바인딩 → KeyMap 선언" 참조

### src/interactive-os/ui/MarkdownViewer.css [M]
- CSS에서 ax() 축 소유 속성 사용 금지 — ax() 또는 해당 축을 사용하세요:
- 상태 선택자 1건 감지:

### src/interactive-os/ui/Spinbutton.tsx
- ui/ 컴포넌트에서 onKeyDown/onKeyUp 바닐라 핸들링 금지 — pattern keyMap 또는 plugin.keyMap으로 선언적 키 매핑을 사용하세요. CLAUDE.md "키바인딩 → KeyMap 선언" 참조

### src/interactive-os/ui/Workspace.tsx
- ui/ 컴포넌트에서 onKeyDown/onKeyUp 바닐라 핸들링 금지 — pattern keyMap 또는 plugin.keyMap으로 선언적 키 매핑을 사용하세요. CLAUDE.md "키바인딩 → KeyMap 선언" 참조

### src/interactive-os/ui/chat/ToolSummaryBlock.tsx
- ui/ 컴포넌트에서 onKeyDown/onKeyUp 바닐라 핸들링 금지 — pattern keyMap 또는 plugin.keyMap으로 선언적 키 매핑을 사용하세요. CLAUDE.md "키바인딩 → KeyMap 선언" 참조
