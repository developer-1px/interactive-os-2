# menubar

> ⬜ 미구현

bar 레벨(horizontal) + dropdown 레벨(vertical) = multi-zone 합성.

## TODO

- [ ] useAriaZone으로 bar + dropdown 두 zone 합성
- [ ] 새 축 불필요 — 기존 navigate(horizontal) + navigate(vertical) 재사용 예상
