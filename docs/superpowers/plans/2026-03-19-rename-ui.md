# Rename UI — Implementation Plan

> PRD: `docs/superpowers/prds/2026-03-19-rename-ui-prd.md`

## Tasks

### Task 1: NodeState.renaming + useAria getNodeState
- `src/interactive-os/hooks/useAria.ts` — getNodeState에서 `__rename__` 엔티티 조회, `renaming: boolean` 추가
- 테스트: getNodeState가 renaming 상태를 올바르게 반환하는지

### Task 2: isEditableElement guard in useAria onKeyDown
- `src/interactive-os/hooks/useAria.ts` — getNodeProps의 onKeyDown에 editable element 가드 추가
- contenteditable, input, textarea가 event.target이면 behavior keyMap 스킵
- Enter → confirmRename, Escape → cancelRename, Tab → preventDefault
- isComposing 가드
- containerProps의 onKeyDown에도 동일 가드 적용 (aria-activedescendant 모드)
- 테스트: editable element에서 키맵 억제 확인

### Task 3: Aria.Editable 컴포넌트
- `src/interactive-os/components/aria-editable.tsx` — 새 파일
- AriaNodeContext + AriaInternalContext에서 nodeId, renaming 읽음
- field prop으로 편집 대상 지정
- renaming=false → children 렌더
- renaming=true → contenteditable div, 텍스트 전체 선택, Enter/Esc/blur 핸들링
- 빈 문자열이면 cancel 처리
- ref 기반 (useState 금지)
- Aria에 Editable로 마운트
- 테스트: contenteditable 전환, confirm/cancel 동작

### Task 4: PageRename 데모 + 더블클릭 진입
- `src/pages/PageRename.tsx` — renderItem에 Aria.Editable 적용
- 더블클릭 rename 진입 (keyMap 또는 Aria.Editable에 onDoubleClick)

### Task 5: Verify
- tsc, eslint, vitest 전체 통과
- /naming-audit, /simplify, code-review
