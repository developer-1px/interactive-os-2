# Retro: followFocus Activation — 2026-03-20

## 비교 기준
- **PRD:** docs/superpowers/specs/2026-03-20-follow-focus-activation-prd.md
- **Diff 범위:** 319bf45 (단일 커밋)
- **커밋 수:** 1
- **변경 파일:** 9 (src 6 + docs 2 + .claude 1)

## 항목별 비교

| # | PRD 항목 | 일치 | 갭 요약 | 계층 |
|---|---------|------|---------|------|
| 1 | 동기 (5개 스토리) | ✅ | 역PRD 4개 스토리가 원본 5개를 정확히 커버 | — |
| 2 | 인터페이스 (7행 + 체크리스트) | ✅ | 역PRD 7행 완전 일치 | — |
| 3 | 산출물 | ✅ | 모든 파일/구조 변경 일치 | — |
| 4 | 경계 (5조건) | ⚠️ | 역PRD가 PRD에 없는 경계 2개 추가 발견 | L2 |
| 5 | 금지 (3항목) | ✅ | 역PRD가 코드에서 금지 사항 정확히 역추론 | — |
| 6 | 검증 (9시나리오) | ❌ | PRD #6~#8 (App 통합 테스트) 미구현 | L1 |

**일치율:** 4/6 완전일치, 1 PRD 누락(⚠️), 1 구현 누락(❌)

## 갭 상세

### ❌ PRD에 있는데 구현 안 됨
- **검증 #6~#8**: ActivityBar 통합 테스트 (레이어 라우트 변경, theme 포커스만 이동, theme Enter 토글)
  - 이들은 App 레벨 통합 테스트로, os 단위 테스트로는 커버 불가
  - e2e 또는 App 컴포넌트 테스트로 별도 작성 필요

### ⚠️ 역PRD에 있는데 PRD에 없었음
- **경계: `newFocusedId`가 빈 문자열일 때** — 코드에 가드 있음, PRD 미기술
- **경계: handler가 activate를 호출하지 않는 keyMap 핸들러** — `intercepted` 플래그로 정상 dispatch, PRD 미기술

## 계층별 개선 제안

### L1 코드 — 백로그
- [ ] 검증 #6~#8 App 통합 테스트 작성 (별도 이슈, 현재 스코프 외)
  - 이유: os 레벨 로직은 13개 테스트로 완전 검증됨. App 통합은 e2e 범위

### L2 PRD 스킬 — 제안
- PRD 경계 조건에 "빈 값/null 입력" 항목을 체크리스트로 추가 권장
  - 이유: `newFocusedId === ''` 가드는 코드에서 자연스럽게 추가됐지만 PRD에 명시되지 않았음
- PRD 경계 조건에 "keyMap handler가 activate를 호출하지 않는 경우" 추가 권장
  - 이유: `dispatchKeyAction`이 activate 외 핸들러도 처리하는데 이 분기가 PRD에 없음

### L5 사용자 피드백
- (없음 — Discussion에서 충분히 정렬됨)

## 다음 행동
- L1 백로그는 App 통합 테스트 범위 → 현재 스코프에서는 스킵
- L2 제안은 사용자 확인 후 PRD 스킬에 반영
