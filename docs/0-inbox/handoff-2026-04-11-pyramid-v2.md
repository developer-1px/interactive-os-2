# Handoff: Pyramid 스킬 + 파서 v2

> 2026-04-11 세션에서 피라미드 파서/스킬을 대폭 개선하고, "AI 시대 프로젝트 가치" 피라미드 도입부를 R1/R2 구조로 재구성

## 완료

| 커밋 | 내용 |
|------|------|
| `a4efe331` | 파서 v2: 레벨별 산문 5단계, R1/R2 타입, 비대 감지, subgraphMap 최적화 |

### 스킬 변경 (플러그인 레포, 미커밋)
- `.claude/skills/pyramid/SKILL.md` — 대폭 개선:
  - R1(원하지 않는 결과)/R2(원하는 결과) 구조 도입: `S→C→R1+R2→Q→A→P+D`
  - 갭 질문 2단계: 초반(빈칸 채우기) / 후반(연결 조이기, Lv.1 최우선)
  - 레드팀 2모드: 화제 열기=반문, 드릴다운=경청+반문
  - 문장 품질 검증: 극단적/모호한/누락 주장 반문
  - 재배치 phase: 팬아웃 5+ 감지 → subgraph 그룹핑 + 순서 정렬 + 추론 유형(귀납/연역) 필수 표기
  - 참조표: 질문 시 관련 노드 ID+문장을 표로 보여줌
  - 완성 판정: 도입부 우선 검증 + 사용자 승인 필수
  - 구조 판단은 LLM의 역할: 사용자에게 구조 질문 금지

### 메모리
- `feedback_redteam_sharper.md` — 레드팀 질문은 반문으로 날카롭게

## 남은 것

### 즉시 (다음 세션 첫 작업)
1. **파서 DFS 순서 수정** — R1+R2→Q 구조에서 R1이 A1 뒤에 중복 출력됨. R1→R2→Q 순서로 읽히도록 DFS 방문 순서 조정 필요
2. **피라미드 도입부 계속 보강** — C1 "상당히" 모호한 표현 수정, Lv.1 산문 흐름 재검증
3. **스킬 플러그인 레포 커밋** — pyramid/SKILL.md 변경분을 plugin-repo에 커밋+푸시

### 이후
- P3→P4 불릿+엣지 중복 출력 정리 (Lv.3에서 P4가 불릿과 엣지로 두 번 나옴)
- 파서에 adjacency map 추가 (edges 반복 필터링 최적화)

## 컨텍스트

- **피라미드 파일**: `docs/0-inbox/pyramid-ai-era-project-value.md` (status: building, 39노드 31엣지)
- **파서**: `scripts/pyramidParse.mjs`
- **스킬**: `.claude/skills/pyramid/SKILL.md`
- **주의**: R1/R2는 민토 피라미드의 도입부 개념. R1=원하지 않는 결과, R2=원하는 결과. Q는 "어떻게 하면 R2를 달성할 수 있을까?"

## 다음 행동 제안

`/pyramid`로 이어서 도입부 보강 + DFS 순서 수정.
