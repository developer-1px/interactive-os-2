좋은 주제입니다. 선행 조사를 먼저 했습니다.

**현재 코드 상태:** dnd 플러그인(`dnd.ts`)은 `moveUp`, `moveDown`, `moveIn`, `moveOut`, `moveTo` 5개 커맨드를 제공하고 있고, 이미 키보드 바인딩은 완비되어 있습니다. 예를 들어 kanban behavior에서 `Alt+Arrow`로 리오더/크로스컬럼 이동이 동작하고, TreeGrid에서도 `Alt+Arrow`로 reparenting이 됩니다. 반면 **마우스 드래그는 코드베이스 전체에 일절 없습니다** — `onDrag`, `draggable`, `useDrag` 등의 흔적이 0입니다.

말씀을 정리하면, "키보드 네비게이션을 DnD에 넣고 싶다"는 것은 키보드 바인딩이 없다는 뜻보다는 **마우스 드래그 앤 드롭을 추가하고 싶다**는 의미 같습니다. 현재 키보드 DnD는 이미 동작하니까요.

혹시 정반대로, 키보드 쪽이 부족하다고 느끼시는 건지 확인이 필요합니다.

---

| 요소 | 내용 | 이해도 |
|------|------|--------|
| 목적 | 마우스만 되는(혹은 키보드만 되는) DnD를 양쪽 모두 지원하고 싶다 | :yellow_circle: 50% |
| 배경 | dnd 플러그인은 커맨드 5종 + 키보드 바인딩 완비, 마우스 드래그 0 | :yellow_circle: 70% |
| 이상적 결과 | ? | :red_circle: 0% |

---

**제 판단: 현재 상태는 "키보드 DnD만 있고 마우스 드래그가 없는" 상태입니다.** 키보드 네비게이션은 kanban(`Alt+Arrow`)과 treegrid(`Alt+Arrow`)에서 이미 동작 중입니다. 그래서 추가해야 할 것은 마우스 포인터 드래그일 가능성이 높습니다.

다르게 보시면 말씀해주세요. 확인을 위해 질문 하나 드립니다:

**"지금 마우스로만 된다"고 하신 건 어떤 화면에서인가요?** 현재 코드에는 마우스 드래그 구현이 없어서, 혹시 외부 앱(Trello, Figma 등)과 비교하면서 "거기는 마우스로 되는데 여기는 아무것도 안 된다"는 맥락인지, 아니면 제가 모르는 별도의 마우스 드래그 코드가 있는 건지 파악하고 싶습니다.

A) 외부 앱 대비 — "칸반 같은 앱은 보통 마우스 드래그가 기본인데, 우리도 추가하고 싶다"
B) 기존 구현이 있음 — "이미 마우스 드래그가 되는 부분이 있는데, 거기에 키보드 대안을 넣고 싶다"
C) 다른 맥락

**제 판단: A일 가능성이 높습니다.** 이유: 코드베이스에 마우스 드래그 구현이 전혀 없고, 프로젝트가 키보드 우선 설계이기 때문입니다.
