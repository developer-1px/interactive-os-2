# 경험 DB 자동 승격 강화 — PRD

> Discussion: 경험 DB → feedback 승격 시 PRD 체크리스트 자동 추가 + 승격 이력 기록

## ① 동기

| # | Given | When | Then | 역PRD |
|---|-------|------|------|-------|
| M1 | 경험 DB에 빈도 3회 이상인 교훈이 존재한다 | retro Step 6에서 빈도 기반 승격이 트리거된다 | feedback memory 파일이 생성되고, MEMORY.md 인덱스가 업데이트되며, **해당 교훈이 관련된 PRD 스킬 체크리스트에 항목이 자동 추가**된다 | |
| M2 | 경험 DB에 빈도 3회 이상인 교훈이 존재한다 | 승격이 완료된다 | 경험 DB의 해당 행에 **승격 이력**(승격 날짜, 생성된 feedback 파일명, 추가된 체크리스트 항목)이 기록된다 | |
| M3 | 승격 이력이 경험 DB에 누적되어 있다 | 다음 retro에서 같은 도메인의 교훈이 다시 등장한다 | 이전 승격 이력을 참조하여 중복 승격을 방지하고, 기존 feedback의 보강 여부를 판단한다 | |

완성도: 🟡

## ② 산출물

> 구조, 관계, 이름 — 파일/컴포넌트/데이터 스키마

| 산출물 | 설명 | 역PRD |
|--------|------|-------|
| `.claude/skills/retro/SKILL.md` 수정 | Step 6 "빈도 기반 승격" 절차에 PRD 체크리스트 자동 추가 단계 삽입 + 승격 이력 기록 포맷 명세 | |
| `.claude/skills/prd/SKILL.md` 수정 | 승격된 교훈이 삽입될 체크리스트 위치(인터페이스 체크리스트 영역 등) 식별 + 동적 추가 가능한 구조로 정비 | |
| `memory/experience_db.md` 스키마 확장 | 기존 테이블에 "승격 이력" 열 추가 (승격 날짜, feedback 파일명, PRD 체크리스트 항목) | |

완성도: 🟡 ← ① 🟡 후 착수

## ③ 인터페이스

> 비-UI 작업이므로 API/절차 입력으로 대체

| 입력 | 현재 상태 | 행동 | 왜 이 결과가 나는가 | 결과 상태 | 역PRD |
|------|----------|------|-------------------|----------|-------|
| retro Step 6에서 빈도 카운트가 3에 도달한 경험 행 | 경험 DB 행: 빈도=3, 승격 이력=없음 | 1. feedback memory 파일 생성 2. MEMORY.md 인덱스 추가 3. **교훈 도메인으로 PRD 스킬 내 관련 체크리스트를 탐색** 4. **매칭되는 체크리스트에 항목 추가** | 삽질이 3회 반복 = 규칙이 될 만큼 패턴화됨. PRD 체크리스트에 반영해야 같은 갭이 재발하지 않음 (K1: 경험 DB → feedback 승격은 '삽질이 규칙이 될 만큼 반복' 신호) | feedback 파일 존재, MEMORY.md 갱신, PRD 체크리스트에 항목 추가됨, 경험 DB에 승격 이력 기록됨 | |
| 승격 완료 직후 | feedback 생성됨, PRD 체크리스트 수정됨 | **경험 DB 행의 승격 이력 열에 기록**: `→ feedback 승격됨 (YYYY-MM-DD, feedback_{name}.md, PRD 체크리스트: {추가 항목})` | 이력이 없으면 다음 retro에서 동일 교훈 재승격 시도 → 중복 파일 + 중복 체크리스트 항목 발생 | 경험 DB 행에 승격 이력이 완전히 기록됨 | |
| 이미 승격된 교훈(승격 이력 열이 채워져 있음)의 빈도가 다시 증가 | 경험 DB 행: 빈도=4+, 승격 이력=있음 | 신규 승격하지 않음. 기존 feedback 파일의 보강 필요 여부만 판단 → 필요 시 feedback 파일 내용 업데이트 | 이미 승격된 교훈은 feedback으로 존재함. 빈도 증가는 feedback 내용이 불충분하다는 신호이지, 새 feedback을 만들라는 신호가 아님 | 기존 feedback 파일이 보강되거나, 변경 불필요로 판단됨 | |

완성도: 🟡 ← ② 🟡 후 착수

## ④ 경계

| 극단 조건 | 현재 상태 | 왜 이렇게 동작해야 하는가 | 예상 동작 | 결과 상태 | 역PRD |
|----------|----------|------------------------|----------|----------|-------|
| 교훈 도메인이 PRD 스킬의 어떤 체크리스트와도 매칭되지 않음 | 경험 DB 행 빈도=3, 도메인="test/vitest" | PRD 스킬에 vitest 관련 체크리스트가 없는 것은 정상. 무관한 체크리스트에 억지로 넣으면 노이즈 증가 | PRD 체크리스트 추가를 건너뜀. 승격 이력에 "PRD 체크리스트: 해당 없음"으로 기록 | feedback 파일은 생성되지만 PRD 체크리스트는 변경 없음 | |
| 같은 retro에서 2개 이상의 교훈이 동시에 빈도 3 도달 | 경험 DB 행 2개 이상이 동시에 빈도=3 | 승격은 독립적 연산이므로 순차 처리해도 부작용 없음 | 각각 독립적으로 승격 절차 수행 (feedback 생성 + 체크리스트 추가 + 이력 기록) | 복수의 feedback 파일 + 복수의 체크리스트 항목 | |
| PRD 체크리스트에 이미 유사한 항목이 존재 | 경험 DB 교훈과 의미적으로 동일한 체크리스트 항목이 이미 있음 | 중복 항목은 체크리스트 품질을 떨어뜨림 | 중복 판정 시 추가하지 않음. 승격 이력에 "PRD 체크리스트: 기존 항목과 중복 — 추가 안 함"으로 기록 | 체크리스트 변경 없음, 이력에 중복 사유 기록 | |
| 승격 이력 열이 이미 있는 행의 빈도가 계속 증가 (4, 5, 6...) | 승격 완료된 행, 빈도=5 | 반복 승격은 무의미. 하지만 빈도 계속 증가는 feedback 내용이 AI 행동을 바꾸지 못한다는 신호 | 빈도만 갱신. 날짜만 갱신. 재승격하지 않음. retro 보고서에 "빈도 높은 기존 feedback 점검 필요" 경고 표시 (?) | 빈도/날짜 갱신, 보고서에 경고 | |

완성도: 🟡 ← ③ 🟡 후 착수

## ⑤ 원칙 대조

| # | 원칙 (출처) | 관련 항목 | 위반 여부 | 위반 시 수정 | 역PRD |
|---|------------|----------|----------|------------|-------|
| P1 | 설계 원칙 > 사용자 요구 충족 (feedback_design_over_request) | ③ 인터페이스 — PRD 체크리스트 자동 수정 | 위반 없음 | — | |
| P2 | PRD 6-item format (feedback_prd_6items) | ② 산출물 — PRD 스킬 수정 | 위반 없음. 현재 PRD 스킬은 8단계 의존 체인 구조이며, 교훈 삽입은 기존 체크리스트 확장으로 6-item 포맷에 영향 없음 | — | |
| P3 | 파일명 = 주 export 식별자 (CLAUDE.md) | ② 산출물 | 위반 없음. 기존 파일 수정이므로 새 파일명 규칙 해당 없음 (feedback 파일은 기존 승격 절차에서 이미 네이밍 규칙 존재) | — | |
| P4 | "삽질이 규칙이 될 만큼 반복" = 승격 신호 (K1) | ① 동기 M1 | 위반 없음. 이 PRD의 핵심 동기와 일치 | — | |
| P5 | L2 수정은 PRD 스킬 체크리스트 추가 (K2) | ③ 인터페이스 | 위반 없음. 이 기능 자체가 K2를 자동화하는 것 | — | |
| P6 | 수정 전 설계 의도 이해 필수 (feedback_fix_requires_design_understanding) | ③ 인터페이스 — 체크리스트 매칭 | 위반 없음. 교훈의 도메인으로 관련 체크리스트를 탐색하므로 맹목적 추가가 아님 | — | |

완성도: 🟡 ← ①~④ 🟡 후 착수

## ⑥ 부작용

| # | 이 기능이 건드리는 기존 것 | 예상 부작용 | 심각도 | 대응 | 역PRD |
|---|------------------------|-----------|--------|------|-------|
| S1 | `retro/SKILL.md` Step 6 절차 | Step 6이 길어져서 retro 전체 실행 시간/복잡도 증가 | 낮 | 허용 — 승격은 드문 이벤트(빈도 3 도달 시만), 추가 단계 2~3개 수준 | |
| S2 | `prd/SKILL.md` 체크리스트 영역 | 체크리스트 항목이 무한 증가할 수 있음 | 중 | ④ 경계에 "중복 판정" 추가 완료. 또한 체크리스트가 과도하게 길어지면 별도 파일로 분리하는 규칙 검토 (?) | |
| S3 | `memory/experience_db.md` 테이블 스키마 | 기존 행에 새 열 추가 → 기존 행은 승격 이력 열이 비어있음 | 낮 | 허용 — 빈 셀은 "아직 승격 안 됨"으로 자연스럽게 해석됨 | |
| S4 | 기존 승격 절차와의 호환 | 현재 승격은 "→ feedback 승격됨" 텍스트만 표시. 새 포맷으로 전환 시 기존 표시와 불일치 | 낮 | 기존 행은 그대로 유지. 새 승격부터 확장 포맷 적용 | |

완성도: 🟡 ← ⑤ 🟡 후 착수

## ⑦ 금지

| # | 하면 안 되는 것 | 출처 (⑤ 위반 / ⑥ 부작용) | 이유 | 역PRD |
|---|---------------|------------------------|------|-------|
| F1 | PRD 스킬의 8단계 의존 체인 구조를 변경하는 것 | ⑤ P2 — 6-item 포맷 보존 | 체크리스트 항목 추가는 기존 구조 내 확장이어야 함. 구조 자체를 바꾸면 모든 PRD 작성에 영향 | |
| F2 | 도메인 매칭 없이 모든 체크리스트에 무차별 추가 | ⑤ P6 — 설계 의도 이해 필수 | 노이즈 증가 → 체크리스트 무시로 이어짐 | |
| F3 | 이미 승격된 교훈을 재승격 (새 feedback 파일 생성) | ⑥ S4 — 기존 절차 호환 | 중복 feedback 파일 → memory 오염, 인덱스 혼란 | |
| F4 | 경험 DB에서 승격된 행을 삭제 | 기존 retro 스킬 규칙 "행은 이력으로 유지" | 승격 이력 추적 불가, 동일 교훈 재등장 시 맥락 상실 | |

완성도: 🟡 ← ⑤⑥ 🟡 후 착수

## ⑧ 검증

| # | 출처 (①동기N / ④경계N) | 시나리오 | 예상 결과 | 역PRD |
|---|----------------------|---------|----------|-------|
| V1 | ①M1 | retro Step 6에서 "prd/연동패턴" 도메인 교훈이 빈도 3에 도달 | feedback 파일 생성 + MEMORY.md 갱신 + PRD 스킬 인터페이스 체크리스트에 "연동 주체 명시 확인" 항목 추가 + 경험 DB에 승격 이력 기록 | |
| V2 | ①M2 | V1 승격 완료 후 경험 DB 확인 | 해당 행에 `→ feedback 승격됨 (2026-03-22, feedback_prd_integration_subject.md, PRD 체크리스트: 인터페이스 — 연동 주체 명시)` 형태의 이력이 기록됨 | |
| V3 | ①M3 + ④경계4 | 이미 승격된 교훈의 빈도가 4로 증가 | 재승격하지 않음. 빈도/날짜만 갱신. 기존 feedback 파일 보강 여부 판단 | |
| V4 | ④경계1 | "test/vitest" 도메인 교훈이 빈도 3 도달. PRD 스킬에 vitest 관련 체크리스트 없음 | feedback 파일은 생성되지만 PRD 체크리스트는 변경 없음. 승격 이력에 "PRD 체크리스트: 해당 없음" 기록 | |
| V5 | ④경계3 | "behavior/role" 도메인 교훈 승격 시 PRD 체크리스트에 이미 "단일 노드 위젯 role 배치 확인" 항목 존재 | 중복으로 판정하여 추가하지 않음. 승격 이력에 중복 사유 기록 | |
| V6 | ④경계2 | 같은 retro에서 교훈 2개가 동시에 빈도 3 도달 | 각각 독립적으로 승격: 2개의 feedback 파일 + 각각의 체크리스트 항목 + 각각의 승격 이력 | |

완성도: 🟡 ← ①~⑦ 🟡 후 착수

---

**전체 완성도:** 🟡 8/8 (AI 초안 완료, 사용자 확인 대기)

## 완성도 갱신

| 단계 | 상태 | 비고 |
|------|------|------|
| ① 동기 | 🟡 | 3개 시나리오 — 기본/이력/중복방지 |
| ② 산출물 | 🟡 | 3개 파일 수정 대상 식별 |
| ③ 인터페이스 | 🟡 | 3개 입력-행동-결과 체인 (승격/이력기록/재승격방지) |
| ④ 경계 | 🟡 | 4개 극단 조건 |
| ⑤ 원칙 대조 | 🟡 | 6개 원칙, 위반 없음 |
| ⑥ 부작용 | 🟡 | 4개 부작용, 심각도 중 1개 (체크리스트 무한증가) |
| ⑦ 금지 | 🟡 | 4개 금지 항목 |
| ⑧ 검증 | 🟡 | 6개 시나리오, 동기×경계 교차 커버 |

## 갭 질문 (AI 자가 진단)

1. **③ 인터페이스**: 교훈 도메인 → PRD 체크리스트 매칭 알고리즘의 구체적 규칙은? 현재는 "도메인으로 탐색"이라 쓰여 있지만, 매칭 기준이 문자열 포함인지, 의미적 판단인지 명확하지 않음. AI 판단: retro/prd 스킬은 LLM이 실행하므로 **의미적 판단**이 자연스러움. 규칙 기반 매칭이 아니라 LLM이 교훈 내용을 읽고 PRD 체크리스트 중 관련 영역을 판단하는 방식이 적절함 (?)

2. **② 산출물**: 승격 이력 열의 정확한 포맷 — 현재 `→ feedback 승격됨` 마커에 추가 정보(날짜, 파일명, 체크리스트 항목)를 덧붙이는 형태. 기존 마커와의 호환성 확인 필요 (?)

3. **⑥ S2**: 체크리스트 항목 상한선을 정할 것인가? AI 판단: 현 시점에서는 과잉 설계. 중복 판정 + 도메인 매칭으로 충분히 제어됨 (?)
